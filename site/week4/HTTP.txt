// this is used when client(browser) is connected to the server(web-site)
GET /index.html?firstName=Yaakov HTTP/1.1 
Method, URI String, Query String HTTP Version

1.  GET - retieves the resource. Data is passed to server as part of URI. sends date to server as part of the URL.
1a. POST - sends data to server in order to be processed. Sends date as part of message body.
1b. response codes: 200(OK), 404(error), 500(error), etc.

HTTP response structure

HTTP/1.1 200 OK // those are separated by single space
HTTP version, Response status code, english phrase describing satus code. 


































